# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_SITE_URL=https://portx-2za.pages.dev

# Product (also editable in src/data/product.ts)
# Booking: set product.bookingUrl to your Cal.com link
# Analytics: set product.analytics.provider to "plausible" | "umami"

# Cloudflare (local Wrangler only — never commit real values)
# CLOUDFLARE_API_TOKEN=
# CLOUDFLARE_ACCOUNT_ID=
